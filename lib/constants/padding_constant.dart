import 'package:flutter/material.dart';

class ProjectPaddings {
  static const cardInPadding = EdgeInsets.only(left: 30, right: 30);
  static const cardOutPadding = EdgeInsets.symmetric(vertical: 170);
  static const paddingAll = EdgeInsets.all(10);
  static const buttonOutPadding = EdgeInsets.only(top: 20);
}
